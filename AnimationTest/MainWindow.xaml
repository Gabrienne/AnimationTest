<Window x:Class="AnimationTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AnimationTest"
        mc:Ignorable="d"
        Title="MainWindow">
    <Grid Background="Black">
        <ItemsControl x:Name="movieGrid" ItemsSource="{Binding Movies}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="MovieItem">
                    <Border Margin="4" Width="160" Height="260" x:Name="movieSlot">
                        <Button Click="clickedMovie">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Width="150" Height="250" x:Name="moviePoster">
                                        <Border.Background>
                                            <ImageBrush ImageSource="{Binding PosterUri}" />
                                        </Border.Background>
                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CommonStates">
                                                <VisualStateGroup.Transitions>
                                                    <VisualTransition To="MouseOver" GeneratedDuration="0:0:0.2"/>
                                                    <VisualTransition To="Pressed" GeneratedDuration="0:0:0.1"/>
                                                </VisualStateGroup.Transitions>
                                                <VisualState x:Name="Normal" />
                                                <VisualState x:Name="MouseOver">
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="moviePoster"
                                                    Storyboard.TargetProperty="Width" 
                                                    To="160" />
                                                        <DoubleAnimation Storyboard.TargetName="moviePoster"
                                                    Storyboard.TargetProperty="Height" 
                                                    To="260" />
                                                    </Storyboard>
                                                </VisualState>
                                                <VisualState x:Name="Pressed">
                                                    <!--<Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="moviePoster"
                                                    Storyboard.TargetProperty="Width" 
                                                    To="255" />
                                                    </Storyboard>-->
                                                </VisualState>
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
